import network
import backgammon_env


def test_1_ply():
    pass
    # n = network.layered(198, 40, 4)
    # b = backgammon_env.Backgammon()
    # state = b.s0()
    # for i in range(100):
    #     m = b.available_moves(state, roll)
